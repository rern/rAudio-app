<resources>
    <!-- Base application theme. -->
    <style name="baseTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/cml</item> <!-- button text -->
        <item name="colorPrimaryVariant">@color/black</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/cml</item>
        <item name="colorSecondaryVariant">@color/black</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/black</item>
        <!-- dialog box -->
        <item name="colorBackgroundFloating">@color/cga</item>
        <item name="android:colorBackgroundFloating">@color/cga</item>
        <item name="android:textColor">@color/cw</item> <!-- title -->
        <item name="android:textColorPrimary">@color/cw</item> <!-- input -->
    </style>

    <style name="splashTheme" parent="Theme.MaterialComponents.NoActionBar">
        <item name="android:windowBackground">@color/black</item>
    </style>
</resources>